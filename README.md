# hf-state-streaming
Ultra-low-latency state streaming &amp; matching engine: Go 1.22+ (gws zero-alloc NextReader) edge ingestion tier + .NET 9 (Native AOT) zero-allocation limit order book over Unix Domain Sockets.
